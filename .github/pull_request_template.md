## What does this change do, and why?

<!-- Link an issue if there is one. -->

## How was this tested?

<!--
- Unit tests added/updated?
- If this touches connection/query/scripting/export logic: did you run the
  relevant #[ignore]'d integration test(s) against a real database? Which?
- Manual testing steps, if any.
-->

## Checklist

- [ ] `cargo fmt`, `cargo clippy --all-targets -- -D warnings`, and `cargo test` pass locally (in `src-tauri/`)
- [ ] `npm run build` passes locally
- [ ] This PR is focused on one thing (a bug fix or one feature, not several unrelated changes)
